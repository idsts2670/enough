---
category: Maintenance
authors: [idsts2670]
---

CI hygiene follow-up to PR #1: format violations across docs and pre-existing `.tsx` files (`yarn lint:fix`), one `void` operator on `ReportCard.tsx`'s navigate call, and 13 `oxlint-disable-next-line no-unused-vars` directives on legacy non-Plaid bank-sync handlers in `loot-core/server/accounts/app.ts` (preserved per PLAN.md OD3 pending Phase 2 demolition).
